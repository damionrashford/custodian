node_modules/
.env
.env.*
!.env.example

# bun test
/coverage/

# Not tracked in this repo (local machine setup / working state, kept on disk only)
/.claude/
/.research/
/.graphify/

# superpowers plugin working state. .superpowers/brainstorm/ holds a live session
# token (.last-token); the plugin only self-ignores .superpowers/sdd/, so ignore the root.
/.superpowers/
/.worktrees/
