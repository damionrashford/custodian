# Vendored: css-tree

`css-tree.mjs` is the unmodified single-file ESM dist of [css-tree](https://github.com/csstree/csstree) v3.2.1, MIT License © by Roman Dvornov. Vendored so the skill has zero npm runtime dependencies (Bun auto-install is disabled under any workspace with a node_modules ancestor).
