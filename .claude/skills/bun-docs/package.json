{
  "name": "bun-docs-skill",
  "private": true,
  "type": "module",
  "description": "Type/install scaffolding for the bun-docs skill. Required only so the IDE resolves Bun + Node types when editing scripts/*.ts. Bun itself doesn't need this to run the scripts.",
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^22"
  }
}
